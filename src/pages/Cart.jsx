import { ToastContainer } from "react-toastify";
import AddCart from "../components/AddCart";

const Cart = () => {
  return (
    <div>
      <ToastContainer />
      <AddCart />
    </div>
  );
};

export default Cart;
