import { Particles } from "@tsparticles/react";
import particlesConfig from "../utils/particlesConfig";

const TsParticles = () => {
  return <Particles options={particlesConfig}></Particles>;
};

export default TsParticles;
